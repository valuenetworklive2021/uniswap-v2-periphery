const waffle = require('ethereum-waffle');
const chai = require('chai');
const { expect } = chai;
const V2Router02 = artifacts.require('ValueswapV2Router02.sol');



chai.use(waffle.solidity);

contract('ValueswapV2Router02::', (accounts) => {
  it('...should be ok.', async () => {
  });
});
